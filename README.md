# WEB SECURITY USING CORS AND SSL/TLS.
The use of cross origin resource sharing and ssl/tls is enable use validate or comfirm which client is actually interfacing with your api.

#installing cors
npm install cors --save


